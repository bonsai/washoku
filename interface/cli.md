# CLI Interface

The CLI is the simplest local interface for research and debugging.

Suggested commands:

```text
washoku search "川崎 2000円 新鮮な魚"
washoku ask "川崎で2000円以内で魚が一番よさそうなのは？"
washoku get restaurant-005
```

The CLI calls the same retrieval layer as REST and MCP.
