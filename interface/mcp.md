# MCP Interface

MCP can expose the same retrieval core as agent tools.

Suggested tools:

- `search_restaurants(query, filters)`
- `get_restaurant(id)`
- `ask_washoku(question)`

MCP is an interface for agents; it does not replace the RAG knowledge model.
